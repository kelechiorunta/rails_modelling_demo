<h1>Login</h1>

<%= form_with model: @user, url: perform_login_path, method: :post, local: true do |f| %>
  <%= f.label :name %>:
  <%= f.text_field :name %><br />

  <%= f.label :email %>:
  <%= f.text_field :email %><br />

  <%= f.submit "Login" %>
<% end %>
