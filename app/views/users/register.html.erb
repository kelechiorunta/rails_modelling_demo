<h1>Sign Up</h1>

<%= form_with model: @user, url: perform_signup_path, method: :post, local: true do |f| %>
  <%= f.label :name %>:
  <%= f.text_field :name %><br />

  <%= f.label :email %>:
  <%= f.text_field :email %><br />

  <%= f.submit "Sign Up" %>
<% end %>
